import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: '100%',
  },
  listContainer: {
    alignItems: 'center',
    zIndex: 0,
    flex: 1,
  },
  createEvent: {
    position: 'absolute',
    bottom: 0,
    width: '100%',
    zIndex: 1,
  },
});
